<button class='btn btn-info btn-lg bottom-flat'>Edit Project</button>
<div class='well'>
<% if @project.errors.any? %>
    <p class='alert alert-danger'>Errors</p>
    <% @project.errors.full_messages.each do |e| %>
        <p class='text-danger'><%= e %></p>
    <% end %>
<% end %>
<%= form_for @project, url: {action: "update"}, html: {class: "nifty_form"} do |f| %>
    <%= f.text_field :name, class: 'form-control', placeholder: 'Name' %>
    <br />
    <%= f.text_area :description, size: "60x12", class: 'form-control', placeholder: 'Description'   %>
    <br />
    <%= f.submit "Edit", class: "btn btn-primary"%>

<% end %>
</div>

